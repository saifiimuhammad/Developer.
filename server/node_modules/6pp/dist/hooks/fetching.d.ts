import { UseFetchDataParams, UseMutionParams } from "../types/hooks.types.js";
declare const useFetchData: <T>({ url, key, dependencyProps, credentials, successCallback, errorCallback, }: UseFetchDataParams<T>) => {
    data: T | undefined;
    loading: boolean;
    error: string;
    refetch: () => void;
    clearCache: () => void;
};
declare const useMutation: <T>({ url, method, credentials, key, successCallback, errorCallback, }: UseMutionParams<T>) => {
    data: T | undefined;
    loading: boolean;
    error: string;
    trigger: (body?: Record<string, unknown>) => Promise<void>;
};
export { useFetchData, useMutation };
